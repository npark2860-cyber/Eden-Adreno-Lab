name: Disabled legacy probe on V47 diagnostic branch

on:
  workflow_dispatch:

jobs:
  disabled:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo "Legacy probe disabled on V47 diagnostic branch"
